# Only needed to preview the site locally (`bundle exec jekyll serve`).
# GitHub Pages builds without it.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
